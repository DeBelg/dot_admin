import React from "react";
import { Paper, Table, TableBody } from "@material-ui/core";

const TableItem = props => (
  <Paper>
    <Table size="medium">
      <TableBody />
    </Table>
  </Paper>
);
